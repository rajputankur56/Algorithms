Linear Search used to find an element from a array, it checks value one by one from array.
Time Complexity : O(N)
Pseudo Code :

for(start to end of array)
{
    if (current_element equals to 5)  
    {
        print (current_index);
    }
}